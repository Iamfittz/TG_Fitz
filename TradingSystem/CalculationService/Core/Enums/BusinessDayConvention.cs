namespace CalculationService.Core.Enums {
    public enum BusinessDayConvention {
        FOLLOWING,
        MODFOLLOWING,
        PRECEDING,
        FRN,
        MODPRECEDING,
        NEAREST,
        NONE,
        NotApplicable
    }
}
